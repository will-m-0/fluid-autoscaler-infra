apiVersion: v1
kind: Service
metadata:
  name: redis-headless
  namespace: fluid-autoscaler
  labels:
    app: redis
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: redis
  ports:
    - name: tcp-redis
      port: 6379
      targetPort: tcp-redis
